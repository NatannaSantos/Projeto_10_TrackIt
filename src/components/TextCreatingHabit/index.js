import { Paragraph } from "./style";

export default function TextCreatingHabit(){    
    return (
        <Paragraph>Você não tem nenhum hábito cadastrado ainda. Adicione um hábito para começar a trackear!</Paragraph>
    );
}